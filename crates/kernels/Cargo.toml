[package]
name = "bcr-kernels"
version = "0.1.0"
edition = "2021"
description = "BCR 高性能 WASM kernel（架构文档 §9.1：wasm-bindgen 模型）"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
blake3 = "1"

[profile.release]
opt-level = 3
lto = true
